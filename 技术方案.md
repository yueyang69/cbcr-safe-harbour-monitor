# CbCR Safe Harbour 预警系统 · 技术方案（V2.3）

> **项目定位**：Transitional CbCR Safe Harbour 风险预警 MVP，**非**补足税计算器  
> **当前版本**：V2.3（HEAD `32d0ca5`，2026-08-25）  
> **核心红线**：AI 永不参与 Safe Harbour 三项计算；所有 AI 输出必经人工确认；LLM 故障不阻塞主流程

---

## 一、技术实现方案

### 1.1 系统架构

四层结构，AI 与税务引擎物理隔离：

```
┌─────────────────────────────────────────────────────────────┐
│  表现层   React 18 · TypeScript · Vite · Tailwind            │
│  子公司上传页 · HQ Dashboard · Approvals 页 · Copilot 浮窗  │
└─────────────────────────────────────────────────────────────┘
                            │  Axios + Header 鉴权
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  API 层    FastAPI · Pydantic v2 · 异步路由                  │
│  /api/upload · /api/mapping · /api/financial-data/* · /api/ai/* │
└─────────────────────────────────────────────────────────────┘
        │                                       │
        ▼                                       ▼
┌──────────────────────────┐      ┌──────────────────────────────┐
│ ⭐ 确定性引擎（红线区）  │      │  AI 增强服务（V2.0+）         │
│  backend/app/services/   │      │  backend/app/services/        │
│  safe_harbour.py         │      │  ai_service.py                │
│  · De minimis            │      │  · 5 项服务                   │
│  · Simplified ETR        │      │  · 5s 超时 + Fallback         │
│  · Routine Profits       │      │  · MiniMax LLM (JSON Mode)   │
│  AI 禁止读取和写入       │      │  · 输出必经 confirmed_by_user │
└──────────────────────────┘      └──────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  数据层   PostgreSQL 15 · SQLAlchemy 2.x 异步 · Alembic     │
│  4 张表 + 2 个 JSONB 字段承载 AI 半结构化数据               │
└─────────────────────────────────────────────────────────────┘
```

---

### 1.2 技术栈

| 层 | 选型 | 一句话理由 |
|---|---|---|
| 后端 | FastAPI + SQLAlchemy async + Pydantic v2 | 异步生态契合税务批量写入 |
| 数据库 | PostgreSQL 15 | JSONB 原生承载 AI 异常标记 |
| 鉴权 | Header `X-User-Role` + `X-Entity-Id` | MVP 阶段无 OAuth 复杂度，V2.3 已加实体范围校验 |
| AI | MiniMax LLM（JSON Mode） | 国内合规，结构化输出便于解析 |
| 前端 | React 18 + Vite + Tailwind + Axios | 类型安全 + 极速冷启动 |
| 部署 | Docker Compose + start.sh + start.bat | 一键启动，跨平台 |
| 测试 | pytest（后端 53 个）+ Vitest（前端 8 个） | 核心引擎 + 实体越权 + AI Mock 全覆盖 |

---

### 1.3 核心引擎：三条硬规则

`backend/app/services/safe_harbour.py` — 纯函数，零依赖，**V2.3 仍保持不变**。

| 测试 | 公式 | 判定 |
|---|---|---|
| **De minimis** | `Revenue ≤ 10M` 且 `PBT ≤ 1M` | 满足即 PASS（豁免） |
| **Simplified ETR** | `(Covered Taxes / PBT) ≥ 阈值` | 阈值按 FY 匹配：2024=15% / 2025=16% / 2026+=17% |
| **Routine Profits** | `PBT ≤ SBIE`，其中 `SBIE = Payroll×10% + Tangible×8%` | SBIE 系数已锁定（commit `cadbafa`） |

**最终判定**：任一 PASS → 整体 PASS；**三项全 FAIL 才 FAIL**。

**关键防御**：
- PBT ≤ 0 时 ETR 直接判 FAIL，不输出 NaN；
- SBIE 字段缺失返回 `INCOMPLETE`（非 False），UI 单独显示"数据不完整"标签；
- 阈值与系数集中在文件顶部常量，2027+ 新阈值仅需改一处。

---

### 1.4 数据库（4 张表 + JSONB）

| 表 | 关键字段 | 用途 |
|---|---|---|
| `companies` | `entity_type` · `parent_entity_id`（V2.3 新增） | 报表实体层级，HQ↔Sub 自引用 |
| `financial_data` | 6 个数值字段 + `ai_anomaly_flags` (JSONB) + `missing_suggestion` (JSONB) | 原始财务行 + AI 半结构化标记 |
| `mapping_rules` | `source_field` → `target_field` + `confidence_score` + `confirmed_by_user` | AI 映射 + 人工放行闸 |
| `jurisdiction_summary` | 三项测试结果 + `final_result` + `computed_at` | 辖区汇总缓存，Dashboard 不重算 |

**核心索引**：`financial_data(company_id, fiscal_year)`（异常同比查询）、`jurisdiction_summary(jurisdiction, fiscal_year)` UNIQUE（缓存一致性）。

---

### 1.5 AI 服务（5 项，三件套保障）

`backend/app/services/ai_service.py` — 每一项都有 **5s 超时 + Fallback + 人工确认** 三件套。

| 服务 | 触发场景 | 输出 | 兜底策略 |
|---|---|---|---|
| **智能字段映射** | 上传时新字段名 | Top 3 候选 + 置信度（JSON Mode） | 走硬编码字典；< 60% 强制人工下拉 |
| **数据异常检测** | 单行财务数据 | 口径 / 波动 / 缺失 三类异常 | 返回空列表，前端无⚠️ |
| **缺失值补全** | 非必填关键字段为空 | 历史中位数 + 来源说明 | 返回空，显示"暂无建议" |
| **风险简报** | HQ Dashboard 顶部按钮 | ≤200 字中文摘要 | 模板降级："N 个辖区，X 个 FAIL" |
| **税务 Copilot** | 右下角浮动窗 | 限定范围的解释 | "AI 暂时不可用，请联系税务顾问" |

**Copilot 系统提示（硬约束）**：

> 只能解释本系统已算出的 PBT / ETR / SBIE / De minimis 结果。禁止补足税计算、避税方案、法律意见。越界一律回复兜底话术。

**越权双重拦截**：调用前做关键词扫描（命中"top-up tax / 补足税"直接拦截），LLM 返回后再扫描一次。

---

### 1.6 V2.3 增量能力（commit `45d4f88` + `32d0ca5`）

| 能力 | 实现 | 业务价值 |
|---|---|---|
| **实体范围限定** | 新增 `X-Entity-Id` Header + `require_entity_scope()` 强制依赖 | 子公司**只能访问自己 `company_id`**，越权 403；scope 检查在 404 查询之前，防探测 |
| **HQ 快捷提交** | `POST /api/financial-data/quick-submit`（仅 HQ/ADMIN） | 总部代子公司一次完成 **upsert + auto-approve + 汇总重建**，Dashboard 即时可见 |
| **关联式审批闭环** | approve / return / delete 均触发 `rebuild_summaries()` | Dashboard 静默自动刷新，无需手动操作 |
| **映射范围隔离** | Subsidiary 仅作用于本次 upload；HQ/Admin 幂等 upsert 共享字典 | 防止子公司误操作污染集团规则 |
| **AI 加固** | 移除硬编码 Key、超时 3s→5s、字段映射无候选自动降级 | `32d0ca5` 收尾，安全与稳定性 |

**Strict 审批流时序**：

```
Subsidiary: draft → update → submit
                                    │
                                    ▼
                          HQ /approvals 页面
                                    │
            ┌──────────┬───────────┴───────────┬──────────┐
            ▼          ▼           ▼           ▼
        approve     return      delete       rebuild_summaries
            │          │           │           （统一触发）
            └──────────┴───────────┴───────────┘
                                    │
                                    ▼
                          Dashboard 静默自动刷新
```

---

### 1.7 部署

```bash
docker compose up -d      # 生产 / 演示
bash start.sh             # macOS / Linux 本地
start.bat                 # Windows 本地
```

详见 `DOCKER_DEPLOYMENT.md`。

---

## 二、技术风险与应对方案

### 2.1 风险矩阵（按优先级）

| # | 风险 | 影响 | 缓解（已落地） |
|---|---|---|---|
| **R1** | 引擎被错误改动导致系统性 PASS/FAIL 偏差 | 🔴 极高 | 模块独立 + 改该文件 PR 必须双复核 + Golden Cases 回归 |
| **R2** | AI 输出错误污染数据库 | 🔴 极高 | `confirmed_by_user` 数据库硬门禁 + 前端统一确认弹窗 + 置信度 < 60% 强制人工 |
| **R3** | AI 越权给出补足税建议 | 🔴 极高 | System Prompt 硬约束 + 调用前后双重关键词过滤 + 免责水印 + 对话日志全留痕 |
| **R4** | LLM 超时 / 不可用 | 🟠 高 | 5s `asyncio.wait_for` + 每项服务的 Fallback 矩阵 + 健康检查端点 + 引擎层完全屏蔽 AI 异常 |
| **R5** | 子公司越权访问他人数据 | 🟠 高（**V2.3 已落地**） | `require_entity_scope()` 强制依赖 + scope 检查在 404 之前 + 所有写路径覆盖 |
| **R6** | 阈值 / SBIE 系数变更未追溯 | 🟡 中 | 集中在常量 + Alembic 版本化 + Dashboard 顶部展示当前生效阈值 |
| **R7** | LLM 调用成本失控 | 🟡 中 | 上下文裁剪（Copilot 仅传当前辖区 JSON）+ `temperature=0.1` + `ai_token_usage` 埋点 |
| **R8** | 并发上传数据错乱 | 🟡 中 | 异步事务 + `jurisdiction_summary` 提交后刷新 + Alembic 迁移加锁 |

---

### 2.2 关键防御机制（详细）

**R1 · 引擎完整性**

- 文件位置：`backend/app/services/safe_harbour.py`
- 评审硬约束：任何修改该文件的 PR 必须在描述中标注 `MODIFIES_SAFE_HARBOUR_ENGINE: YES`，由业务 + 技术双 Reviewer 通过
- 单元测试覆盖：ETR 边界值（15.00% / 14.99% / 16.01%）、De minimis 临界（10M / 10M+1）、SBIE 系数、PBT=0 分母保护、任一 PASS 整体 PASS 逻辑

**R2 · AI 输出可靠性**

```sql
-- 数据库硬门禁示例
ALTER TABLE mapping_rules
ADD CONSTRAINT chk_ai_confirmed
CHECK (confidence_score IS NULL OR confirmed_by_user = TRUE);
```

未确认的 AI 推荐在所有业务查询中被自动过滤，从存储层杜绝污染。

**R4 · LLM 降级路径**

```
LLM 调用 ──┬── 5s 超时 ──┐
           │             │
           ├── 4xx ──────┤
           │             │
           ├── 5xx ── 1 次退避重试
           │             │
           └── 其他 ─────┴──► 触发 Fallback 分支（每服务独立的降级输出）
                                     │
                                     └─► safe_harbour_engine 完全不感知 AI 状态
```

**R5 · 实体越权（V2.3 已落地）**

```python
# backend/app/dependencies.py
def require_entity_scope(role, entity_id, company_id):
    if role != Role.SUBSIDIARY:
        return
    if not entity_id:
        raise HTTPException(400, "X-Entity-Id header required")
    if entity_id != company_id:
        raise HTTPException(403, "Subsidiary restricted to its own entity")
```

应用范围：财务数据 CRUD、AI 端点、公司列表、Approve / Return / Delete。

---

### 2.3 测试覆盖现状

| 层级 | 数量 | 覆盖 |
|---|---|---|
| 后端 pytest | **53** | 引擎边界、AI Mock、实体越权、quick-submit、映射 scope |
| 前端 Vitest | **8** | ConfirmDialog、AnomalyBadge、Copilot、Approvals UI |

---

### 2.4 演进路径

| 方向 | 风险 | 对策 |
|---|---|---|
| 新财年阈值（2027+） | 改 `ETR_THRESHOLDS` 字典 | 走 PR + Golden Cases 回归 |
| 多币种 | 汇率折算误差 | 引入独立 `fx_service`，记账 / 报送币种分离 |
| 替换 LLM 厂商 | Prompt 兼容性 | 抽象 `LLMClient` 接口 |
| 多集团隔离 | 数据串集团 | `group_id` 字段 + PostgreSQL Row-Level Security |
| 真实 CbCR XML 接入 | Schema 复杂度 | upload_service 加 XML 解析层，引擎不动 |

---

## 核心承诺

> 所有税务计算只走 `safe_harbour.py` 一个纯确定性模块；  
> 所有 AI 输出必经 `confirmed_by_user=TRUE` 才进入业务流；  
> 所有 LLM 调用都有 5s 超时与独立 Fallback，AI 全挂不影响三项测试；  
> 所有子公司越权访问在依赖层被硬拦截。

这是 V2.3（`32d0ca5`）从架构层面给出的承诺，也是比赛演示稳定可复现的工程基础。