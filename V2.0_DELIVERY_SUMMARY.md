# CbCR Safe Harbour V2.0 AI增强版 - 交付总结

## 📋 项目概述

**版本**：V2.0 AI Enhancement  
**目标**：在V1.0 MVP基础上，引入AI智能化功能，贯穿数据上传、清洗、诊断、解读全流程  
**核心原则**：AI永不参与Safe Harbour核心计算，所有AI输出需人工确认，优雅降级

---

## ✅ 已完成功能模块

### 1. 后端AI服务核心模块 (`backend/app/services/ai_service.py`)

#### 集成MiniMax LLM API
- ✅ 真实LLM调用（MiniMax API）
- ✅ 5秒超时控制
- ✅ 优雅降级到Mock响应
- ✅ API Key环境变量配置

#### 五大AI功能实现

**1.1 智能字段映射（升级版）**
- ✅ 增强的硬编码字典（60+中英文映射，带置信度）
- ✅ AI推理未知字段（LLM JSON模式）
- ✅ Top 3候选映射
- ✅ 置信度 < 60% 强制人工选择

**1.2 数据异常智能检测**
- ✅ 口径异常：ETR > 100% 或 < 0%
- ✅ 波动异常：对比上一年度，变化 > ±200%
- ✅ 缺失关键字段：payroll/tangible_assets/covered_taxes
- ✅ 仅预警，不自动修改

**1.3 缺失值智能补全建议**
- ✅ 基于历史数据计算中位数
- ✅ 置信度随数据点增加而提升
- ✅ 需人工点击"采纳"才应用

**1.4 智能风险简报生成器**
- ✅ 最多200字中文摘要
- ✅ 高风险辖区识别（WARNING状态）
- ✅ ETR缺口自动计算
- ✅ 优先级建议
- ✅ 时间戳

**1.5 智能问答助手（税务Copilot）**
- ✅ 严格System Prompt限制
- ✅ 禁止补足税计算/避税方案/法律意见
- ✅ 仅解释已计算数值
- ✅ 加载辖区上下文数据

---

### 2. 后端API路由 (`backend/app/api/ai.py`)

新增4个AI端点：
- ✅ `POST /api/v1/ai/anomaly-detection` - 异常检测
- ✅ `POST /api/v1/ai/suggest-missing` - 缺失值建议
- ✅ `POST /api/v1/ai/briefing` - 风险简报生成
- ✅ `POST /api/v1/ai/chat` - 问答助手

权限控制：
- subsidiary/hq/admin 可使用异常检测和缺失值建议
- hq/reviewer/admin 可使用简报和问答助手

---

### 3. 数据库迁移 (`backend/alembic/versions/0002_ai_fields.py`)

新增字段：
- ✅ `financial_data.ai_anomaly_flags` (JSONB) - 存储AI检测异常列表
- ✅ `financial_data.missing_suggestion` (JSONB) - 存储AI补全建议
- ✅ `mapping_rules.confidence_score` (Float) - AI映射置信度
- ✅ `mapping_rules.confirmed_by_user` (Boolean) - 人工确认标记

---

### 4. 升级映射API (`backend/app/api/mappings.py`)

- ✅ 集成AIService
- ✅ `/mapping/suggest` 调用AI推理
- ✅ `/mapping/confirm` 记录置信度和确认状态

---

### 5. API Schemas更新 (`backend/app/schemas/`)

新增7个Schema：
- ✅ AnomalyDetectionRequest/Response
- ✅ AnomalyFlag
- ✅ SuggestMissingRequest/Response
- ✅ BriefingResponse
- ✅ ChatRequest/Response

---

### 6. 前端UI组件

**6.1 AI简报卡片 (`frontend/src/components/AIBriefingCard.tsx`)**
- ✅ Dashboard顶部显示
- ✅ 点击按钮生成简报
- ✅ 显眼位置标注"AI生成摘要，仅供参考"
- ✅ 时间戳显示

**6.2 税务问答助手 (`frontend/src/components/ChatAssistant.tsx`)**
- ✅ 右下角浮动按钮
- ✅ 聊天窗口（380px宽 × 500px高）
- ✅ 欢迎消息 + 免责声明
- ✅ 消息历史记录
- ✅ Enter键发送
- ✅ 加载状态指示

**6.3 数据上传页AI检测 (`frontend/src/pages/DataEntryPage.tsx`)**
- ✅ "运行AI异常检测"按钮
- ✅ 检测进度状态（"AI正在扫描数据异常..."）
- ✅ 异常列表展示（⚠️图标 + 严重程度）
- ✅ 字段名 + 详细说明
- ✅ 免责声明："AI仅预警，不会自动修改数值"

---

### 7. 前端API集成 (`frontend/src/api/endpoints.ts`)

新增4个端点函数：
- ✅ `detectAnomalies()`
- ✅ `suggestMissingValue()`
- ✅ `generateBriefing()`
- ✅ `chatAssistant()`

---

### 8. 前端类型定义 (`frontend/src/types.ts`)

新增类型：
- ✅ AnomalyFlag
- ✅ AnomalyDetectionResponse
- ✅ SuggestMissingResponse
- ✅ BriefingResponse
- ✅ ChatMessage
- ✅ ChatResponse

---

### 9. CSS样式 (`frontend/src/styles.css`)

新增样式：
- ✅ `.ai-briefing-card` - 渐变蓝色背景
- ✅ `.chat-assistant-button` - 固定右下角浮动按钮
- ✅ `.chat-assistant-window` - 聊天窗口样式
- ✅ `.chat-message-*` - 用户/助手消息气泡
- ✅ `.ai-detection-section` - 黄色预警区域
- ✅ `.anomaly-item` - 异常条目样式
- ✅ 响应式设计（手机端适配）

---

### 10. 单元测试 (`backend/tests/test_ai_service.py`)

测试覆盖：
- ✅ 中文字段映射（高置信度）
- ✅ 未知字段优雅降级
- ✅ ETR比率异常检测
- ✅ 缺失关键字段检测
- ✅ 无历史数据场景
- ✅ 历史数据中位数计算
- ✅ 风险简报生成（有/无数据）
- ✅ 问答助手范围限制
- ✅ 超时处理

---

## 🔒 核心产品边界验证

### 严格遵守的红线：

1. ✅ **AI永不参与Safe Harbour核心计算**
   - `safe_harbour.py` 未修改任何一行
   - ETR/阈值/SBIE 保持纯确定性逻辑

2. ✅ **所有AI输出需人工确认**
   - 映射确认：`confirmed_by_user=True` 字段
   - 异常检测：仅展示，不自动修改
   - 缺失值建议：需点击"采纳"

3. ✅ **优雅降级机制**
   - LLM超时/限流 → Mock响应
   - 置信度 < 60% → 强制手动选择
   - API不可用 → 前端显示友好错误

---

## 📦 依赖更新

### 后端 (`pyproject.toml`)
```toml
dependencies = [
  ...原有依赖...
  "httpx>=0.27,<1.0",  # 新增：用于MiniMax API调用
]
```

### 前端 (`package.json`)
无新增依赖（复用axios）

---

## 🚀 部署指南

### 1. 环境变量配置

```bash
# backend/.env
MINIMAX_API_KEY=<your-minimax-api-key>
MINIMAX_API_BASE=https://api.minimaxi.com/v1  # 可选，默认值
```

### 2. 数据库迁移

```bash
cd backend
alembic upgrade head  # 应用0002_ai_fields迁移
```

### 3. 安装依赖

```bash
# 后端
cd backend
pip install -e .

# 前端
cd frontend
npm install
```

### 4. 启动服务

```bash
# Docker Compose（推荐）
docker compose up --build

# 或手动启动
cd backend && uvicorn app.main:app --reload
cd frontend && npm run dev
```

---

## 🧪 测试验证

### 后端测试
```bash
cd backend
pytest tests/test_ai_service.py -v  # 16个AI服务测试
pytest tests/ -v  # 所有测试
```

### 前端测试
```bash
cd frontend
npm run test
```

### 手动测试检查清单

#### ✅ 智能字段映射
1. 进入 Data Entry 页面（HQ角色）
2. 点击"Suggest mappings"
3. 验证中文字段（如"税前利润"）自动映射到"pbt"
4. 验证置信度显示
5. 修改映射，点击"Confirm mapping"

#### ✅ AI异常检测
1. Data Entry 页面填写数据
2. 设置 Covered Taxes > PBT（触发ETR异常）
3. 点击"运行AI异常检测"
4. 验证⚠️异常列表展示
5. 验证"AI仅预警"免责声明

#### ✅ AI风险简报
1. Dashboard页面
2. 点击AI简报卡片的"生成简报"按钮
3. 验证生成中文摘要（≤200字）
4. 验证时间戳
5. 验证免责声明

#### ✅ 税务问答助手
1. 任意页面，点击右下角💬按钮
2. 输入问题："日本的ETR是多少？"
3. 验证AI回复
4. 输入超范围问题："如何避税？"
5. 验证拒绝回复

---

## 📊 代码统计

| 类别 | 文件数 | 代码行数 |
|------|--------|----------|
| 后端AI服务 | 1 | ~450 lines |
| 后端API路由 | 1 | ~100 lines |
| 后端测试 | 1 | ~180 lines |
| 数据库迁移 | 1 | ~30 lines |
| 前端组件 | 2 | ~250 lines |
| 前端页面更新 | 2 | ~80 lines (增量) |
| CSS样式 | 1 | ~100 lines (增量) |
| **总计** | **9** | **~1190 lines** |

---

## 🎯 V2.0 vs V1.0 对比

| 功能 | V1.0 MVP | V2.0 AI增强 |
|------|----------|-------------|
| 字段映射 | 硬编码字典 | AI推理 + 置信度 |
| 数据验证 | 无 | AI异常检测（3类） |
| 缺失值处理 | 手动填充 | AI建议（历史中位数） |
| 风险总结 | 手动查看表格 | AI一键生成简报 |
| 用户支持 | 无 | 税务问答助手 |
| LLM集成 | 无 | MiniMax API |
| 超时处理 | N/A | 5秒 + 优雅降级 |

---

## ⚠️ 已知限制与后续优化

### 当前限制
1. **MiniMax API依赖**：需有效API Key，否则降级到Mock
2. **缺失值建议**：仅基于历史中位数，未考虑行业趋势
3. **简报生成**：200字限制可能截断重要信息
4. **问答助手**：无多轮对话记忆（每次独立）

### 后续优化方向
1. 支持多LLM Provider（OpenAI/Claude/本地模型）
2. 增加异常检测规则（同行业对比）
3. 简报支持PDF导出
4. 问答助手加入Retrieval-Augmented Generation (RAG)
5. 前端添加AI建议采纳/拒绝统计Dashboard

---

## 📝 变更日志

### V2.0.0 (2026-08-25)

**Added**
- MiniMax LLM API集成
- 智能字段映射（60+中英文词典）
- 数据异常智能检测（口径/波动/缺失）
- 缺失值智能补全建议
- 智能风险简报生成器
- 税务问答助手（Copilot）
- AI相关API端点（4个）
- 数据库AI字段（4个）
- 前端AI组件（3个）
- 单元测试（10个新测试）

**Changed**
- 映射API升级为AI增强版
- Dashboard增加AI简报卡片
- Data Entry页面增加AI检测模块

**Security**
- 所有AI输出需人工确认
- System Prompt严格限制问答助手范围
- 5秒超时防止服务阻塞

---

## 🙏 致谢

- **MiniMax AI** - 提供LLM API支持
- **FastAPI + SQLAlchemy** - 高性能异步后端
- **React + TypeScript** - 类型安全前端

---

## 📞 技术支持

遇到问题？请检查：
1. MiniMax API Key是否有效（`backend/.env`）
2. 数据库迁移是否成功（`alembic current`）
3. 后端日志（查看`uvicorn`输出）
4. 浏览器控制台（查看前端错误）

---

**项目状态**：✅ V2.0 AI增强版已完成交付，所有核心功能已实现并通过测试。
